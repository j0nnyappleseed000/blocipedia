<% if wiki %>
  <tr>
    <td><%= link_to wiki.title, wiki_path(current_user) %></td>
    <td><%= wiki.updated_at %></td>
    <td><%= link_to "edit", edit_wiki_path(current_user) %></td>
  </tr>
<% else %>
<% end %>